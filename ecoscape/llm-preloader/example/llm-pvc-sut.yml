apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecoscape-pvc
  namespace: sut
spec:
  storageClassName: local-path
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
